# 工作台 App 更新说明

## 本次更新内容

1. **新增「热点」新闻模块** —— 每天抓取国内外、娱乐、社会热点新闻
   - 来源：ABC News（头条 / 国际 / 娱乐）+ NPR（世界 / 文艺），共 5 个源
   - 已打包 50 条最新新闻到 `data/feed.json`，离线也能看

2. **商务英语发音模块** —— 现在每个例句 / 单词旁都有 🔊 发音按钮，点击朗读；底部可调语速

3. 数据已刷新到最新（物业 50 条 / 双碳 50 条 / 新闻 50 条）

## 本次带「热点」的正确文件

解压后得到 `workbench-app` 文件夹，**进入这个文件夹**，里面的这些就是全部需要上传的文件：

```
workbench-app/
├── index.html      ← 页面（含热点标签）
├── app.js          ← 逻辑（含新闻加载）
├── manifest.json   ← PWA 配置
├── icon-192.png
├── icon-512.png
├── icon.svg
├── data/
│   └── feed.json   ← 数据（含 50 条新闻）
├── README.md
├── serve.py        ← （本地服务器，网页端不需要）
├── refresh_data.py ← （本地刷新脚本，网页端不需要）
└── tunnel.sh       ← （本地隧道，网页端不需要）
```

> 如果之前上传的是「整个 workbench-app 文件夹」导致打不开，这次请务必**进入文件夹、把里面文件直接拖到仓库根目录**。

## 上传到 GitHub（手机操作）

1. 手机下载本 zip 并解压，进入 `workbench-app` 文件夹
2. 打开 GitHub → 你的仓库 `camuskiss/workbench-SS` → **Add file → Upload files**
3. 把 `workbench-app` 文件夹**里面的文件全部选中上传**（关键：`index.html`、`app.js`、`data/feed.json`）
4. 遇到同名冲突选「覆盖 Overwrite」
5. 底部填说明（如 `add hot news`），点 **Commit changes**
6. 等 1~2 分钟，打开 `https://camuskiss.github.io/workbench-SS/`，底部应有 6 个标签：**今日 · 物业 · 双碳 · 热点 · 英语 · 演讲**

## 如果还是看不到「热点」

- **强制刷新**：把网址完全关掉重新打开，或清一下浏览器缓存（旧版被缓存了）
- **检查文件层级**：确认 `index.html` 直接在仓库根目录，没有套 `workbench-app/` 层
- **检查 GitHub Pages**：仓库 Settings → Pages，确认已启用、分支选的是 `main`

## 数据更新机制

- `data/feed.json` 是打包好的最新数据，保证打开就有内容
- 网页每次打开会尝试从 RSS 源拉取最新数据，联网会自动更新
- 想彻底手动刷新：电脑上运行 `python3 refresh_data.py` 重新抓取，再提交推送

## 安全提醒

你的 GitHub token（`ghp_...`）如果还留着，建议在 GitHub 里撤销它：Settings → Developer settings → Personal access tokens → Delete。
